brokerClusterName = DefaultCluster
brokerName = broker-a
brokerId = 0
deleteWhen = 04
fileReservedTime = 48
brokerRole = ASYNC_MASTER
flushDiskType = ASYNC_FLUSH
# Docker 内部客户端场景下不要写死宿主机局域网 IP，
# 让 Broker 使用当前容器网络地址注册到 NameServer，避免换网后失效。
